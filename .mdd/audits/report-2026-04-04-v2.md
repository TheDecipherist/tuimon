# MDD Audit Report v2 — 2026-04-04

## Executive Summary

Full audit of all new code since v1 audit. The quick modes and layout system are functional but have significant code quality and security issues that need attention before publishing as a stable release.

**Total: 69 findings — 3 Critical (XSS), 6 High, 21 Medium, 39 Low**

**3 files exceed 300-line limit:** generator.ts (696), modsec.ts (388), index.ts (319)

## Files Over 300 Lines

| File | Lines | Action |
|------|-------|--------|
| src/layout/generator.ts | 696 | Split into generator-css.ts, generator-js.ts, generator-html.ts |
| src/quick/parsers/modsec.ts | 388 | Extract stats builder to shared module |
| src/index.ts | 319 | Extract resize handler + render loop |

## Findings by Severity

### CRITICAL (3)

| # | File | Finding |
|---|------|---------|
| C1 | generator.ts | XSS: theme values injected into JS strings unsanitized — `'${t.success}'` |
| C2 | generator.ts | XSS: `innerHTML` in event-log/status-grid injects user data without escaping |
| C3 | generator.ts | XSS: table `innerHTML` injects column names and cell values without escaping |

### HIGH (6)

| # | File | Finding |
|---|------|---------|
| H1 | generator.ts | 696 lines — exceeds 300-line limit, needs splitting |
| H2 | modsec.ts | 388 lines — exceeds 300-line limit |
| H3 | index.ts | 319 lines — exceeds 300-line limit |
| H4 | index.ts | Render timeout (5s) force-unlocks mutex — causes garbled output if render is still running |
| H5 | auto-layout.ts | `liveLayout` returns empty layout — throws in `validateLayout` |
| H6 | file-mode.ts | Zero test coverage for entire file |

### MEDIUM (21)

| # | File | Finding |
|---|------|---------|
| M1 | csv.ts | No support for RFC 4180 multiline quoted fields |
| M2 | log.ts | Memory guard checks character count not byte count |
| M3 | log.ts | Reads entire file into memory before size check |
| M4 | modsec.ts | `parseJsonModSec` unsafe casts without runtime validation |
| M5 | modsec.ts | `blockedRequests` double-counts intercepted + 4xx/5xx |
| M6 | file-mode.ts | Reads log files twice (detect format + parse) |
| M7 | watch-mode.ts | No error handling in URL fetch `dataFn` closure |
| M8 | cli.ts | Stdin temp file never cleaned up |
| M9 | cli.ts | `--columns ""` produces `[""]` instead of undefined |
| M10 | index.ts | Nav key JS string interpolation fragile (safe now but pattern risk) |
| M11 | detect.ts | Content-based CSV detection could misclassify files |
| M12 | detect.ts | Silent error swallowing in `detectByContent` |
| M13 | generator.ts | `data.max = 0` silently becomes 100 in gauge |
| M14 | generator.ts | Theme precedence: `layout.theme` overrides `themeOverrides` entirely |
| M15 | auto-layout.ts | Table `dataToWidgetData` caps at 100 rows — pagination supports more |
| M16 | watch-mode.ts | Zero test coverage |
| M17 | cli.ts | Zero test coverage |
| M18 | json.ts | JSONL fallback swallows intentional errors (empty array → confusing message) |
| M19 | json.ts/csv.ts | `detectMeta` duplicated across both files |
| M20 | modsec.ts | Dead regex variables (`RULE_MSG_RE`, `RULE_SEV_RE`) |
| M21 | file-mode.ts | `process.exit(0)` in F10 bypasses watcher cleanup |

### LOW (39)

Deferred — highlights: titleFromFilename should use path.basename, version hardcoded, missing edge case tests across all parsers, magic number thresholds.

## Test Coverage

| Module | Tests | Coverage |
|--------|-------|----------|
| Existing core (8 files) | 125 | Good |
| quick-detect | 10 | Partial (no content-detection tests) |
| quick-parsers (json/csv) | 15 | Partial |
| quick-parsers-log (log/modsec) | 19 | Partial |
| quick-auto-layout | 16 | Partial (no live/log data tests) |
| layout-generator | 22 | Good |
| layout-theme | 7 | Good |
| file-mode.ts | 0 | **None** |
| watch-mode.ts | 0 | **None** |
| cli.ts | 0 | **None** |
| index.ts (orchestrator) | 9 | Minimal |

## Fix Plan

### Phase 1 — Critical XSS Fixes (~15 min)
1. Add HTML escaping to all `innerHTML` injections in generator.ts JS
2. Sanitize theme values before injecting into JS strings
3. Escape table column names and cell values

### Phase 2 — High Severity (~30 min)
4. Split generator.ts into 3 files
5. Extract modsec stats to shared module
6. Extract resize/render from index.ts
7. Fix render timeout race condition
8. Fix liveLayout empty validation

### Phase 3 — Medium Fixes (~45 min)
9. Fix double file read in file-mode
10. Fix stdin temp cleanup in CLI
11. Fix columns empty string handling
12. Remove auto-layout 100-row cap
13. Extract shared detectMeta
14. Remove dead modsec regex
15. Add error handling to watch-mode URL fetch
