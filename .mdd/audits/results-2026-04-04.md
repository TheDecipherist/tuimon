# MDD Audit Fix Results — 2026-04-04

## Summary

**Fixed: 21 findings (7 HIGH + 14 MEDIUM)**
**Skipped: 2 MEDIUM (M3 acceptable, M11 complex/low-impact)**
**Remaining: 34 LOW (deferred)**

Tests: 96/96 passing | Typecheck: Clean

---

## HIGH Fixes (7/7)

| # | Fix Applied |
|---|-------------|
| H1 | server.ts: Path traversal fix — use `resolve()` + trailing `sep` comparison |
| H2 | keyhandler.ts: Guard `setRawMode` with `isTTY` check, added `pause()` on stop |
| H3 | index.ts: Added `.catch()` on `router.handleKey()` call |
| H4 | index.ts: Circuit breaker — 5 consecutive failures disables auto-refresh, `unref()` interval |
| H5 | encoder.ts: Sixel streams output per-band instead of building entire string |
| H6 | encoder.ts: Log warning when palette exceeds 256 colors |
| H7 | router.ts: F-key actions now awaited with try/catch instead of fire-and-forget |

## MEDIUM Fixes (14/16)

| # | Fix Applied |
|---|-------------|
| M1 | detect.ts: Query lock serializes stdin queries to prevent race conditions |
| M2 | server.ts: Wrapped `decodeURIComponent` in try/catch, returns 400 |
| M4 | browser.ts: Crash handler limited to 3 retries with counter |
| M5 | encoder.ts: `renderKitty` changed from `async` to synchronous `void` |
| M6 | encoder.ts: `sharp()` call wrapped in try/catch with error log |
| M7 | router.ts: `getPageHtml` throws descriptive error instead of non-null assertion |
| M8 | router.ts: Removed redundant ternary — uses `state.pageId` directly |
| M9 | keyhandler.ts: `stop()` now calls `process.stdin.pause()` |
| M10 | fkeybar.ts: Removed dead `plain` variable from `buildBar()` |
| M12 | index.ts: SIGINT handler now calls `stop().catch()` instead of `void stop()` |
| M13 | index.ts: Render mutex — `rendering` flag prevents concurrent `renderFrame` |
| M14 | index.ts: Validates pages not empty before accessing `[0]` |
| M15 | cli.ts: Added `unhandledRejection` and `uncaughtException` handlers |
| M16 | — Skipped: `__tuimon_ready__` sync flag is a design limitation, not a bug |

## Skipped

| # | Reason |
|---|--------|
| M3 | Sync `readFileSync` in server — acceptable for local dev tool |
| M11 | Multi-byte char slicing in `notify()` — low impact, complex fix |
